<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.login_retro">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ.GSERVICES" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Api.model.Ongoing_Mission"
            android:label="Ongoing Mission">

        </activity>
        <activity
            android:name=".Api.model.Execute_Mission"
            android:label="Execute Mission"/>
        <activity
            android:name=".Api.model.Web"
            android:label="Monitoring"/>
        <activity android:name=".Api.model.Missions"
            android:label="Missions"/>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBdPcmJ62Ivps_pqPVHQQMG2iMDPQSnND4" />

        <activity
            android:name=".Api.model.Track_Drone"
            android:label="Drone Location" />
        <activity
            android:name=".Api.model.Create_Mission"
            android:label="Delivery"/>
        <activity
            android:name=".Api.model.Delivery_Drone"
            android:finishOnCloseSystemDialogs="true"
            android:label="Drone Status"/>
        <activity
            android:name=".Api.model.Drones"
            android:label="Drones"/>
        <activity android:name=".Api.model.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>